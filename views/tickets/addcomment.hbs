<h3>Add Comment</h3>
  <div class="row">
    <form action="/tickets/addComment/{{ticket._id}}" method="POST" class="col s12 m4">
      {{!-- <input type="hidden" name="_method" value="PUT" /> --}}
      <div class="row">
        <div class="input-field">
          <input type="text" id="comments" name="comments" value="{{ticket.comments}}" />
          <label for="comments">Comment</label>
        </div>
      </div>

      <div class="row">
        <input type="submit" value="Save" class="btn" />
        <a href="/tickets" class="btn orange">Cancel</a>
      </div>
    </form>
  </div>